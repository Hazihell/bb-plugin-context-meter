{
  "name": "bb-plugin-context-meter",
  "version": "0.1.6",
  "type": "module",
  "engines": {
    "bb": ">=0.38",
    "bbPluginSdk": ">=0.4.6"
  },
  "bb": {
    "name": "Context Meter",
    "description": "Keeps threads in the smart zone, by tracking context against a limit you set rather than the provider's full window.",
    "branding": {
      "icon": "./icon.svg"
    },
    "server": "./server.ts",
    "app": "./app.tsx"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@get-bb/plugin-sdk": "0.4.6",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "better-sqlite3": "^12.0.0",
    "hono": "^4.11.9",
    "typescript": "^5.7.0"
  },
  "description": "Keeps threads in the smart zone, by tracking context against a limit you set rather than the provider's full window.",
  "license": "MIT",
  "files": [
    "dist/",
    "server.ts",
    "app.tsx",
    "icon.svg",
    "README.md"
  ],
  "scripts": {
    "build": "bb plugin build",
    "typecheck": "tsc --noEmit"
  }
}
